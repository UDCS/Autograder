server:
  port: 8080
  
db:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: autograder
  sslmode: disable

auth:
  jwtsecret: secret # Change this to a long random string
  admin: [admin@udallas.edu] # TODO: invite all given users to be admin when application is set up
